/**
 * Community exploration heuristics (see `elite_dangerous_exploration_payout_estimator.ts`–style scripts).
 * Primary credits use MattG / EDMC Pioneer in `explorationValue.ts`; only the DSS probe-efficiency tail uses this multiplier.
 */
export const DSS_PROBE_EFFICIENCY_MULT = 1.25;
